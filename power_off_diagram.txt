@startuml

title "Power off"
left to right direction

actor User
boundary "Power button" as powerkey
boundary GUI
control OS #lightgreen
control PowerManager #cyan
control CameraManager #cyan
control PlaybackController #cyan

User --> powerkey : "press"
powerkey -> OS
OS -> PowerManager : "key event"
PowerManager -> GUI : "confirm action"
GUI -> User
PowerManager --> CameraManager
PowerManager --> PlaybackController

@enduml