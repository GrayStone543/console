@startuml

title "Power Off"

actor User
boundary "Power Button" as powerkey
entity Console
entity OS

User -> powerkey : press <Power button>
powerkey -> OS
OS -> Console : key event
Console -> User : message window
User -> Console : confirmed
Console -> OS : power off
OS -> OS : power off

newpage

title "Power Off"
actor User
boundary "Power Button" as powerkey
entity Console
entity OS

User -> powerkey : press <Power button>
powerkey -> OS
OS -> Console : key event
Console -> User : message window
User -> Console : confirmed
Console -> OS : stop recording
Console -> OS : power off
OS -> OS : power off

@enduml